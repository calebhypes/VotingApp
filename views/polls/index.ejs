<% include ../partials/header %>
    <main>
        <% include ../partials/recent %>

        <section class="top10 polls">
            <h3>Top 10 Polls</h3>
            <ul>
                <% popular.forEach((poll) => { %>
                    <a href="/polls/<%= poll._id %>"><li><%= poll.question %></li></a>
                <% }) %>
            </ul>
        </section>

        <% include ../partials/allpolls %>
    </main>

<% include ../partials/footer %>